# Full dev environment: pipeline scripts + API, for local work.
# Deployment uses requirements-api.txt instead (see Dockerfile) -- the API
# only needs fastapi/uvicorn/numpy/pydantic at serving time.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.0
pydantic==2.7.4
pyarrow==16.1.0
nltk==3.10.0
requests==2.32.3
dnspython==2.6.1
